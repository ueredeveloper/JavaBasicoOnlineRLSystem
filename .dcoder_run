# write commands to install modules/lib and run your project, one in each line.
# java, javac and gradle commands are available along with other basic linux commands.
# command to compile the project
# replace ${FILE_NAME} with your main file

##javac -Xlint -encoding UTF-8 -cp .:./lib/* Main

# command to run,  replace ${MODULE} with generated class output file from above command

##java -Xmx256m -Dfile.encoding=UTF-8 -cp .:./lib/* Main

#search all .java, put in .txt and compile
###find -name "*.java" > sources.txt && javac -encoding UTF-8 -d bin @sources.txt
# run .class in bin past
##java -Xmx256m -Dfile.encoding=UTF-8 -cp bin main.Main

## desconsiderar modificações e puxar tudo 
## do github 
### git reset --hard 

## 27/10/20
###git pull
##git status

##27/10/20
##git reset --hard 
##git pull

git add .
git status 